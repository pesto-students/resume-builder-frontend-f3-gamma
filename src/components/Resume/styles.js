import styled from "styled-components";

const Wrapper = styled.div`
  padding: 16px 0;
  border-bottom: 0px solid var(--lightgrey);
  h2 {
    color: var(--color);
  }
`;

export { Wrapper };